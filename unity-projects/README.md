# Unity Runner Projects

This directory is reserved for small runner projects.

The first checked-in baseline is intentionally lightweight. Add Unity 2020 and Unity 6 runner projects here when the package tracks are ready for editor validation.
