# Nexus Game Kit Test Runs

Cloud validation harness for Nexus Game Kit. This repository installs the publishable Nexus Game Kit package into Unity runner projects, executes queue-based smoke payloads, captures logs/artifacts, and produces validation reports.

See `QUICKSTART.md` and `RUNBOOK.md` for usage.
